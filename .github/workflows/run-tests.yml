name: Run Tests
run-name: Build & Test Project Across 16+ Test Cases 🚀
on: [push]
jobs:
  ExecuteTests:
    runs-on: ubuntu-latest
    steps:
      - run: echo $(ls)
      - run: cargo build -r
      - run: cd tests
      - run: cargo run
